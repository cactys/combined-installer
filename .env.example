# ============================================================
#  Combined Installer — Configuration template
#  Copy to .env and fill in your values:
#    cp .env.example .env
# ============================================================

# ── NAS / File Server ────────────────────────────────────────
#
#  Option 1: Synology WebDAV (recommended)
#    Enable in DSM > Control Panel > File Services > WebDAV
#    NAS_BASE=https://your-nas.synology.me:5006
#
#  Option 2: Synology via local network
#    NAS_BASE=https://192.168.1.100:5006
#
#  Option 3: SMB/UNC share
#    NAS_BASE=\\192.168.1.100\software
#
#  Option 4: Plain HTTP/HTTPS file server
#    NAS_BASE=https://your-server.com/installers
#
NAS_BASE=https://your-nas.synology.me:5006

# Synology DSM account with read access to the software folder
# For SMB — Windows credentials or leave empty for anonymous
NAS_USER=installer_user
NAS_PASS=your_password

# ── Local paths ──────────────────────────────────────────────
# Temp folder for downloaded installers (auto-created, cleaned after install)
TEMP_DIR=%TEMP%\CombinedInstaller

# Log file — kept after installation, never deleted
LOG_FILE=%TEMP%\CombinedInstaller\install_log.txt

# ── UI Labels ────────────────────────────────────────────────
APP_TITLE=Combined Installer
APP_SUBTITLE=Automatic software installation

# ── Windows Defender exclusions ──────────────────────────────
# Comma-separated paths and processes to whitelist during install
DEFENDER_EXCLUDE_PATHS=C:\Program Files\1cv8,C:\Program Files (x86)\AnyDesk
DEFENDER_EXCLUDE_PROCESSES=1cv8.exe