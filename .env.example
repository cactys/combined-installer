# ============================================================
#  Company Installer — Конфигурация окружения
#  ВАЖНО: этот файл НЕ должен попасть в Git!
#         Добавлен в .gitignore автоматически.
#
#  Список приложений — в apps.json (можно коммитить)
# ============================================================

# --- NAS / Облачное хранилище ---
# HTTP/HTTPS (Nextcloud, S3, Backblaze, прямые ссылки):
NAS_BASE=https://your-nas.example.com/installers
# ИЛИ SMB (локальная сеть):
# NAS_BASE=\\192.168.1.100\installers

# --- Авторизация на NAS (если нужна) ---
# Оставь пустым если авторизация не нужна
NAS_USER=
NAS_PASS=

# --- Пути на локальной машине ---
TEMP_DIR=%TEMP%\CombinedInstaller
LOG_FILE=%TEMP%\CombinedInstaller\install_log.txt

# --- Настройки окна ---
APP_TITLE=Combined Installer
APP_SUBTITLE=Автоматическая установка корпоративного ПО
APP_COMPANY=Your Company Name
APP_VERSION=1.0.0

# --- Исключения Windows Defender ---
# Через запятую, без пробелов
DEFENDER_EXCLUDE_PATHS=C:\Program Files\1cv8,C:\Program Files (x86)\AnyDesk
DEFENDER_EXCLUDE_PROCESSES=1cv8.exe